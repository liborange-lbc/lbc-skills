# Code Review Report

> **Change** `<name>` · **分支/Commit** `<branch>` / `<commit>` · **日期** `<YYYY-MM-DD>` · **审查者** AI
>
> **AI**：等级 **P0 / P1 / P2**；G/S 以 checklist 行内定义为准；Bug 模式以 `bug-pattern-checklist.md` 表头为准（Blocker→P0、Major→P1、Info→P2）。**须先**运行 `scan-all-rules.sh` 并将要点并入 §5，**再**写 LLM 结论。问题须含 `path:line` 或清单 ID：可读性 `A3.4`，安全 `S1.1`，可靠性 `G16.2`，Bug 模式 `B012` / `M005` 等。

---

## 1. 审查范围

| 项 | 值 |
|----|-----|
| `.java` 文件数 | `<N>` |
| 变更行数 | `+<a> / -<r>` |

| 类/接口 | 路径 | 角色（可选） |
|---------|------|--------------|
| `<Name>` | `<path/File.java>` | `<简述>` |

---

## 2. 问题计数

| P0 | P1 | P2 |
|----|----|-----|
| 0 | 0 | 0 |

---

## 3. Step 2 — 功能（REQ）

### REQ-x: `<名称>`

| Scenario | 结果 | Spec证据 | 代码证据 | 说明 |
|----------|------|----------|----------|------|
| `…` | ✅ / ❌ / ⚠️ | `<原文摘录 / 章节>` | `<path:line / 测试 / 接口>` | |

---

## 4. Step 3 — 可读性检查

> 无 Java：**N/A**。

| 结果 | 说明（违规写 Ax.x 与 `path:行`） |
|------|--------------------------------|
| ✅ / ❌ / N/A | |

---

## 5. Step 4 — 可靠性检查

| 域 | 参考 | 结果 | 等级 | 说明（列命中 ID 或「已扫无命中」） |
|----|------|------|------|-------------------------------------|
| 可靠性 | `reliability-checklist.md` G1–G17 | ✅/❌/N/A | P0–P2 | G14/G18 按需 |
| 安全 | `security-checklist.md` S1–S10 | ✅/❌/N/A | P0–P1 | |
| Bug 模式 | `bug-pattern-checklist.md` B/M/I（120） | ✅/❌/N/A | P0–P2 | 预扫：`scan-all-rules.sh`；命中写 `B012` 等 + `path:line` |

---

## 6. Step 5 — 自定义扩展检查

| 域 | 参考 | 结果 | 等级 | 说明（列命中 ID 或「未启用自定义规则」） |
|----|------|------|------|------------------------------------------|
| 自定义扩展 | `customized-checklist.md` U* | ✅/❌/N/A | P0–P2 | 团队/项目自定义规则 |

---

## 7. 结论

- **合并建议**：通过 / 修复后合并 / 阻止合并
- **P0**：1. …
- **P1/P2**：1. …
- **一句话**：`<风险与质量>`

---

## 8. 修复任务列表

> **用途**：供后续改代码时逐项执行与核销；须与 §3–§7 中 ❌/⚠️ 及结论中的可执行项对应。**无待办**时保留本小节，正文写一行：`- 无待修复项。`

**书写规则**：

- 使用 Markdown 任务项 `- [ ]`，修复完成后可改为 `- [x]` 或删除该行。
- 每条一行：**等级** + **定位**（`path:行号` 或清单 ID，如 `S2.1` / `G5.3` / `B012` / `M005`）+ **可执行动作**（动词开头、可独立完成）。
- **排序**：先 **P0**，再 **P1**，最后 **P2**；同等级内按路径/ID 字母序。

### P0

- [ ] **P0** `<path/File.java>:<line>` — `<动作>`
- [ ] **P0** `<S2.1 等>` — `<动作>`

### P1

- [ ] **P1** `<path/File.java>:<line>` — `<动作>`

### P2（可选）

- [ ] **P2** `<path/File.java>:<line>` — `<动作>`
